## To run the app

Install dependencies:
`yarn install`

Start app:
`yarn start`

## Change .env file

for Windows:
`SASS_PATH=./node_modules;./src/assets/styles`

for MacOS:
`SASS_PATH=node_modules:src/assets/styles`
